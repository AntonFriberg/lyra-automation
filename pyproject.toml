[project]
name = "lyra-automation"
version = "0.1.0"
description = "Extract guest-apartment bookings from Lyra Smart Brf calendar"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "playwright>=1.60.0",
]

[dependency-groups]
dev = [
    "pytest>=9.1.0",
]
